# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development: &DEVELOPMENT
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: isavant_development
  pool: 5
  username: root
  password:

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &TEST
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: isavant_test
  pool: 5
  username: root
  password:

production: &PRODUCTION
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: isavant_production
  pool: 5
  username: root
  password:
